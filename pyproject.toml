[project]
name = "multi-agent-patterns"
version = "0.1.0"
description = "Four patterns for building multi-agent AI systems"
readme = "README.md"
requires-python = ">=3.13.3"
dependencies = [
    "langchain-openai>=1.1.6",
    "langgraph>=1.0.5",
    "python-dotenv>=1.2.1",
]
